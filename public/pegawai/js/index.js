(async()=>{try{const u=await boot('Beranda','index.html',`<div id="dash"><div class="dashboard-loading"><span></span><span></span><span></span><b>Memuat dashboard...</b></div></div>`);const d=await api('/api/dashboard'),s=d.summary||{},central=['bos','admin_pusat'].includes(u.role);const branchRows=d.branchSummary||[],logs=central?(await api('/api/activity-logs')).logs||[]:[];const recent=(d.recent||[]).map(x=>`<tr><td><b>${esc(x.invoice_no||'-')}</b><small>${dateID(x.created_at)}</small></td><td>${esc(x.branch_name||'-')}</td><td>${esc(x.full_name||'-')}</td><td><span class="pay-badge ${x.payment_method==='kasbon'?'warn':''}">${esc((x.payment_method||'tunai').toUpperCase())}</span></td><td><b>${money(x.total)}</b></td><td><button class="btn btn-outline btn-small detail-dash" data-id="${x.id||''}">Detail</button></td></tr>`).join('')||`<tr><td colspan="6" class="empty">Belum ada transaksi hari ini.</td></tr>`;const branchHtml=branchRows.map(x=>`<tr><td><b>${esc(x.name)}</b><small>${esc(x.code||'')}</small></td><td><span class="badge ok">${esc(x.status||'aktif')}</span></td><td>${Number(x.transactions||0)}</td><td>${money(x.sales||0)}</td><td>${Number(x.low_stock||0)?`<span class="badge warn">${x.low_stock} produk</span>`:'<span class="badge ok">0</span>'}</td><td><div class="progress"><span style="width:${Math.min(100,Math.max(5,Number(x.sales||0)/(Math.max(1,Number(s.sales||1)))*100))}%"></span></div></td></tr>`).join('')||`<tr><td colspan="6" class="empty">Belum ada data cabang.</td></tr>`;const logHtml=logs.slice(0,6).map(x=>`<div class="activity-item"><span class="activity-avatar">${esc((x.full_name||x.username||'S')[0].toUpperCase())}</span><div><b>${esc(x.full_name||x.username||'Sistem')}</b><small>${esc(x.description||x.action||'-')}</small></div><time>${x.created_at?new Date(x.created_at).toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit'}):'-'}</time></div>`).join('')||`<div class="empty">Belum ada aktivitas sistem.</div>`;const days=(d.dailySales||[]);const max=Math.max(1,...days.map(x=>Number(x.sales||0)));const chart=days.map(x=>`<div class="chart-col"><div class="chart-value">${money(x.sales)}</div><div class="chart-bar" style="height:${Math.max(8,Number(x.sales||0)/max*100)}%"></div><small>${new Date(x.day).toLocaleDateString('id-ID',{day:'2-digit',month:'short'})}</small></div>`).join('')||'<div class="empty">Belum ada data penjualan 7 hari.</div>';const pay=(d.paymentSummary||[]).map(x=>`<div class="pay-row"><span>${esc((x.payment_method||'').toUpperCase())}</span><b>${money(x.sales)}</b><small>${x.transactions} trx</small></div>`).join('')||'<div class="empty">Belum ada pembayaran hari ini.</div>';
document.getElementById('dash').innerHTML=`<div class="page-head dashboard-head"><div><div class="eyebrow">${central?'RINGKASAN BISNIS':'RINGKASAN OPERASIONAL'}</div><h1>${central?'Ringkasan seluruh bisnis':'Ringkasan operasional'}</h1><p>${central?'Data gabungan cabang aktif dan pusat':'Data operasional cabang Anda'} · ${new Date().toLocaleDateString('id-ID',{day:'numeric',month:'long',year:'numeric'})}</p></div><div class="actions"><a class="btn btn-outline" href="laporan.html">Buka laporan</a><button class="btn primary" id="exportDash">Unduh ringkasan</button></div></div>
<div class="cards dashboard-stat-grid"><div class="stat"><span class="label">Penjualan hari ini</span><div class="value">${money(s.sales)}</div><span class="hint">${Number(s.transactions||0)} transaksi</span></div><div class="stat"><span class="label">Transaksi</span><div class="value">${Number(s.transactions||0)}</div><span class="hint">Selesai hari ini</span></div><div class="stat"><span class="label">Produk aktif</span><div class="value">${Number(s.products||0)}</div><span class="hint">Produk terdaftar</span></div><div class="stat"><span class="label">Pelanggan</span><div class="value">${Number(s.customers||0)}</div><span class="hint">Data pelanggan</span></div><div class="stat"><span class="label">Pegawai aktif</span><div class="value">${Number(s.employees||0)}</div><span class="hint">Akun aktif</span></div><div class="stat ${Number(s.lowStock||0)>0?'stat-warning':''}"><span class="label">Stok minimum</span><div class="value">${Number(s.lowStock||0)}</div><span class="hint">${Number(s.lowStock||0)>0?'Perlu diperiksa':'Semua aman'}</span></div><div class="stat"><span class="label">Total stok</span><div class="value">${Number(s.stock||0).toLocaleString('id-ID')}</div><span class="hint">Unit tersedia</span></div><div class="stat ${Number(s.kasbon||0)>0?'stat-warning':''}"><span class="label">Kasbon belum lunas</span><div class="value">${money(s.kasbon||0)}</div><span class="hint">Saldo berjalan</span></div></div>
<div class="dashboard-two"><div class="panel"><div class="panel-head"><div><b>Penjualan 7 Hari</b><small>Pergerakan penjualan berdasarkan transaksi selesai</small></div></div><div class="chart">${chart}</div></div><div class="panel"><div class="panel-head"><div><b>Metode Pembayaran</b><small>Penjualan hari ini</small></div></div><div class="panel-body payment-summary">${pay}</div></div></div>
${central?`<div class="panel dashboard-branch"><div class="panel-head"><div><b>Ringkasan per cabang</b><small>Kinerja dan kondisi stok tiap cabang hari ini</small></div><span class="updated">Diperbarui ${new Date().toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit'})}</span></div><div class="table-wrap"><table class="table branch-table"><thead><tr><th>Cabang</th><th>Status</th><th>Transaksi</th><th>Penjualan</th><th>Stok minimum</th><th>Kontribusi</th></tr></thead><tbody>${branchHtml}</tbody></table></div></div>`:''}
<div class="dashboard-bottom"><div class="panel"><div class="panel-head"><div><b>Transaksi terbaru</b><small>Penjualan terakhir yang tercatat</small></div><a class="btn btn-outline btn-small" href="riwayat.html">Lihat semua</a></div><div class="table-wrap"><table class="table recent-table"><thead><tr><th>No. transaksi</th><th>Cabang</th><th>Kasir</th><th>Pembayaran</th><th>Total</th><th></th></tr></thead><tbody>${recent}</tbody></table></div></div>${central?`<div class="panel activity-panel"><div class="panel-head"><div><b>Aktivitas sistem</b><small>Aktivitas pengguna terbaru</small></div></div><div class="activity-list">${logHtml}</div></div>`:`<div class="panel"><div class="panel-head"><div><b>Status stok</b><small>Kondisi stok cabang Anda</small></div><a class="btn btn-outline btn-small" href="stok.html">Cek stok</a></div><div class="panel-body stock-focus"><div><strong>${Number(s.stock||0).toLocaleString('id-ID')}</strong><span>unit tersedia</span></div><div class="stock-alert ${Number(s.lowStock||0)?'warn':''}">${Number(s.lowStock||0)} produk berada pada atau di bawah stok minimum.</div></div></div>`}</div>`;
document.querySelectorAll('.detail-dash').forEach(b=>b.onclick=()=>location.href='riwayat.html');$('#exportDash').onclick=()=>window.print();}catch(e){const d=document.getElementById('dash');if(d)d.innerHTML=`<div class="panel error-panel"><b>Dashboard tidak dapat dimuat.</b><p>${esc(e.message||'Terjadi kesalahan.')}</p><button class="btn primary" onclick="location.reload()">Muat ulang</button></div>`}})();
