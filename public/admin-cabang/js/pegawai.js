(async()=>{await boot('Pegawai Cabang','pegawai.html',`<div class="page-head"><div><div class="eyebrow">TIM CABANG</div><h1>Pegawai Cabang</h1><p>Melihat pegawai dan status akun pada cabang Anda.</p></div></div><div class="panel"><div class="table-wrap"><table class="table"><thead><tr><th>Username</th><th>Nama</th><th>Role</th><th>Status</th></tr></thead><tbody id="rows"></tbody></table></div></div>`);const data=(await api('/api/users')).users;$('#rows').innerHTML=data.map(x=>`<tr><td><b>${esc(x.username)}</b></td><td>${esc(x.full_name)}</td><td><span class="badge info">${esc(x.role.replace('_',' '))}</span></td><td><span class="badge ${x.status==='aktif'?'ok':'danger'}">${x.status}</span></td></tr>`).join('')||'<tr><td colspan="4" class="empty">Belum ada pegawai.</td></tr>'})().catch(e=>toast(e.message));
